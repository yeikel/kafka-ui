name: "Infra: Release: Serde API"

on:
  workflow_dispatch:
    inputs:
      version:
        required: true
        type: string
        description: Serde API version to release. Must not be a SNAPSHOT.
        default: 1.x.0

permissions:
  contents: read

jobs:
  release-serde-api:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # https://github.com/actions/checkout/releases/tag/v6.0.3
        with:
          fetch-depth: 0
          token: ${{ github.token }}

      - run: |
          git config user.name github-actions
          git config user.email github-actions@github.com

      - name: Set up JDK
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # https://github.com/actions/setup-java/releases/tag/v4
        with:
          java-version-file: '.java-version'
          distribution: 'zulu'
          cache: 'gradle'

      - name: Validate version
        run: |
          VERSION="${{ inputs.version }}"
          if echo "$VERSION" | grep -qi 'SNAPSHOT'; then
            echo "::error::Version must not contain SNAPSHOT: $VERSION"
            exit 1
          fi
          if ! echo "$VERSION" | grep -Pq '^\d+\.\d+\.\d+$'; then
            echo "::error::Version must be valid semver (e.g. 1.0.0): $VERSION"
            exit 1
          fi

      - name: Publish to Maven Central
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
        run: |
          ./gradlew publishToSonatype \
          closeAndReleaseSonatypeStagingRepository \
          -Prelease=true \
          -Pversion=${{ inputs.version }}
